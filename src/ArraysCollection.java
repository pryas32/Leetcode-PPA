public class ArraysCollection {

    //Arrays->its a utility functions that provide functions for nirmal arrays not for collection.

    //meothods like
    //.asList()->it provide list over same array
    //.compare();
    //mismatch(arr1,arr2)
    //.fill()
     //Arrays.sort()->sort primitive type only in increasing order
    //binary_search()
    //toString()->print the array in one go
    //stream()
    //equals()








}
